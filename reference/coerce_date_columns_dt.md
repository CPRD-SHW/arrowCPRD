# Coerce dates in data.table

Coerce dates in data.table

## Usage

``` r
coerce_date_columns_dt(data_in, date_cols = NULL)
```

## Arguments

- data_in:

  Dataset passed in

- date_cols:

  Character vector of column names to cast to date
