# Schemas for reading in Aurum data by types

Schemas for reading in Aurum data by types

## Usage

``` r
aurum_observation_schemas

aurum_linkage_schemas

aurum_patient_schemas

aurum_practice_schemas

aurum_problem_schemas

aurum_referral_schemas

aurum_drug_schemas

aurum_consultation_schemas

aurum_staff_schemas
```

## Format

An object of class `list` of length 3.

An object of class `list` of length 1.

An object of class `list` of length 3.

An object of class `list` of length 3.

An object of class `list` of length 3.

An object of class `list` of length 3.

An object of class `list` of length 3.

An object of class `list` of length 3.

An object of class `list` of length 3.
